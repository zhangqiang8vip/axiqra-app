# Axiqra Frontend Local Environment Example
#
# Copy this file to `.env` (which is git-ignored) and adjust the values
# to match your local backend setup. The defaults below work with the
# dev profile on http://localhost:8080.

VITE_API_BASE_URL=http://localhost:8080/api